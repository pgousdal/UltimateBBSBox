kind: service
schema_version: 1
id: mail-edge
service:
  type: infrastructure
  title: UBB Mail Edge
  family: postfix
exposure:
  main_menu: false
  admin_only: true
endpoint: mail-edge
lifecycle:
  mode: always_on
  sharing: multiuser
  restart: on_failure
