---
backup_dest_dir: "/var/backups/mystic"
backup_retention_days: 14
backup_schedule: "0 3 * * *"        # informational (cron-style) description
backup_oncalendar: "*-*-* 03:00:00" # actual systemd timer schedule - keep in sync with backup_schedule above
backup_remote_target: ""
