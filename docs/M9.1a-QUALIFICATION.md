# M9.1a real DOSEMU2 + FreeDOS qualification

## Result: INCOMPLETE

This qualification was run on the available test host on the recorded delivery
date. The host is Ubuntu 26.04 LTS, Linux kernel `7.0.0-29-generic`, x86_64.
No `dosemu`/`dosemu2` executable is installed, no supported DOSEMU2 package is
available in the configured package metadata, and no approved M1-preserved
FreeDOS image or installer is present in the repository/archive workspace.
QEMU 10.2.1 is present, but it is an alternative backend and cannot be used to
claim DOSEMU2 qualification.

Consequently, no real FreeDOS boot, headless boot marker, COM1 byte exchange,
disconnect, persistence, crash/reconcile, multiple-instance, or DOS locking
result is claimed. The qualification CLI records these as `HUMAN_REQUIRED` and
does not install software or download media:

```text
python3 scripts/ubb-dos.py runtime-info
python3 scripts/ubb-dos.py qualify
```

The qualification framework requires an explicit guest-produced marker
(`UBB_DOS_READY`); process liveness alone is not evidence. It records backend,
host architecture, profile, timestamp, state, reason, and safe evidence fields.

## What remains applicable

M9.1's product-neutral profile, golden/working separation, path isolation,
generic COM/PTy model, node allocator, M3/M4/M5 boundaries, and FreeDOS
preservation workflow remain valid. No proprietary DOS media, BBS product,
physical serial device, public network, or production state was touched.

When a disposable host provides DOSEMU2 and an authoritative FreeDOS artifact,
the next qualification must acquire/preserve the artifact through M1, derive an
immutable golden environment, clone a working copy, boot with an explicit marker,
exercise COM1 byte-for-byte (including CP437/ANSI/CRLF), test clean stop/restart
and at least two isolated instances, and record filesystem-locking limitations.
Historical BBS compatibility remains `HUMAN_REQUIRED` even after those tests.
