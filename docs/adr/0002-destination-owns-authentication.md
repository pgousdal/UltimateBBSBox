# ADR 0002: Destination owns caller authentication

Status: Accepted

UBB does not require callers to log in before choosing a destination. A BBS, MUD, shell, or other destination normally performs its own authentication. Session routing is not shared identity.
