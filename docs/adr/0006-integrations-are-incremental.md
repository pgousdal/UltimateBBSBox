# ADR 0006: Integrations are incremental and may require manual steps

Status: Accepted

UBB may catalog hundreds of historical programs, but runnable integrations are implemented and qualified one at a time. `assisted` and `manual_install` are valid automation levels and manual steps must declare evidence needed to continue.
